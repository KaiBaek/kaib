#include <stdio.h>

int main(void) {

    int score; // 변수를 선언 = 상자 소환!
    score = 62; // 변수를 초기화(처음 값을 넣은것)

        printf("%d",score);  // 오른쪽 - 츌력하고 싶은 값, 왼쪽 - 위치/
        printf("score is %d", score);
        printf("num is  %d.", score);

    return 0;
}
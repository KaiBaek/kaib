#include <stdio.h>

int main(void) {

// char c;
// scanf_s("%c", &c);
// printf("%c",c);

int num;
scanf("%d", &num);

if(num>10){

    printf("num is larger than 10.", num);

} else{ 

    printf("num is equal or smaller than 10.", num);
}

    return 0;
}